# We extends from actor script so it means that it has all the functionallity of the Actor script
# I think it works as import in python or smth like that
extends Actor  # Inherit from the Actor class

# Now, player inherits all the functionality of Actor, including _physics_process()
